<li class="dropdown">
    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true"
       aria-expanded="false">{{ 'recipe.categories'|trans }} <span class="caret"></span></a>
    <ul class="dropdown-menu">
        {% for board in boards %}
            <li><a id="{{ board.id }}" class="page-scroll"
                   href="{{ path('showBoard', {'slug': board.title}) }}">{{ board.title }}</a></li>
        {% endfor %}
    </ul>
</li>